import { exec } from "child_process";
import { resolve } from "path";

export function fetchMetadataFromSkopeo(imageName: string): Promise<object> {
    return new Promise((resolve, reject) => {
        
    })
}